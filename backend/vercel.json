{
	"version": 2,
	"buildCommand": "npm run build",
	"builds": [
		{
			"src": "dist/server.bundle.js",
			"use": "@vercel/node",
			"config": {
				"includeFiles": ["dist/**"]
			}
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "dist/server.bundle.js"
		}
	]
}
